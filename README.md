# Dog Breed Classification with Vision Transformer (ViT) Model

This project implements a deep learning model using the Vision Transformer (ViT) to classify images of dogs into 120 different breeds. Leveraging transfer learning and a pre-trained ViT model, the system achieves high accuracy on real-world datasets with varying image quality and subtle inter-breed differences.
